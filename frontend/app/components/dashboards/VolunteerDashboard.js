"use client";
import { motion } from "framer-motion";
import ProblemCard from "../ProblemCard";
import Link from "next/link";

export default function VolunteerDashboard({ problems = [], userLoc }) {
  // Logic remains for potential future use or children, but the UI requested for removal is gone.
  return (
    <div className="space-y-8">
      {/* Cards removed as per professional dashboard upgrade request */}
    </div>
  );
}